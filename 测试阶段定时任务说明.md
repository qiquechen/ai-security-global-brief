# 测试阶段定时任务

配置日期：2026-09-09（同日按最新要求调整触发时间为“整点发送、半点采集”）。使用 Windows 任务计划程序，当前用户登录且电脑开机时运行。

| 任务名 | 触发时间（本机时间，北京时间） | 操作 |
| --- | --- | --- |
| AISecurityBrief-Test-crawl | 每小时 30 分 | 采集最近2天的真实来源，成功后立即筛选、摘要并准备下一整点附件 |
| AISecurityBrief-Test-send | 每小时 00 分 | 只读取本小时就绪文件，校验并发送已准备附件 |

半点（30分）开始采集，成功后立即以最近48小时数据准备邮件文件，写入下一整点专用的 `data/ready-test-YYYYMMDD-HH.txt`。整点只读取该批次并发送，不调用模型。如果半点批次尚未准备完成或失败，整点任务记录错误，不发送历史文件；晚完成的批次保留供人工处理，不自动顺延或补发。同一任务尚未结束时跳过新的触发。

每次报告保存到独立目录。控制台日志位于 `logs/test-send-*.log` 或 `logs/test-crawl-*.log`；阶段耗时、结果、黑名单跳过数等追加到 `logs/operations-YYYYMMDD.jsonl`（UTC日期）。

测试窗口可能重复包含此前已收录文章；剔除文章受 7 天记录拦截，但目前没有“已发送文章永久排除”机制。采集与准备按顺序执行，整点只消费准备成功的本期文件。

安装脚本：`scripts/install_test_tasks.ps1 -Python <Python解释器绝对路径>`。执行包装：`scripts/run_scheduled_test.ps1`。正式日报安装脚本保持原有用途。

暂停测试任务：

```powershell
Disable-ScheduledTask -TaskName 'AISecurityBrief-Test-crawl'
Disable-ScheduledTask -TaskName 'AISecurityBrief-Test-send'
```

恢复时使用 `Enable-ScheduledTask`。这两个任务没有自动结束日期，测试结束后应暂停。

配置时发现 `AIDigestDaily`、`AIDigestTestSend` 指向另一个旧项目目录，未修改这两个旧任务；它们可能继续按原计划运行。
