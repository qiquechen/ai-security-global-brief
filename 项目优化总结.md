# 项目近五小时优化总结

> 本文前两部分保留各阶段历史记录；涉及当前来源数量、机制和测试结果时，以本节及
> `docs/2026-09-14_融合与机制改进记录.md` 为准。

## 最新状态：2026-09-14 远端与本地融合

当前 `main` 已快进到队友远端提交，并融合本地尚未提交的 Crawl4AI、采集统计和扩源改动。
本轮不是覆盖式合并：远端的判定重试、摘要事实核验、失败告警、数据库备份和来源诊断均保留；
本地的 LLM-Native Crawler、日期观察缓存、文章 URL 正则、淘汰原因统计和 12 个来源均保留。

| 方向 | 融合后的状态 | 作用 |
| --- | --- | --- |
| 数据源 | 144 项配置、111 项启用、33 项停用 | 远端 15 项与本地 12 项扩源取并集 |
| 来源结构 | 31 gov、2 gov_affiliated、52 thinktank、22 media、37 organization | 媒体与机构两类同时扩充 |
| 采集方式 | 106 page、37 RSS、1 Sitemap | 优先使用稳定 Feed，保留专题页与站点地图 |
| 浏览器增强 | Crawl4AI `off/fallback/always` | 静态失败或弱正文时转换渲染 DOM 为精简 Markdown |
| 预抓取统计 | `url_filtered`、`hint_outside`、`blacklisted` | 解释 raw 候选在正文请求前被淘汰的原因 |
| 历史缓存 | `crawl_observations` | 避免小时任务反复请求已确认的窗口外旧文 |
| 筛选容错 | 失败条目整批结束后重试一次 | 网络抖动不立即导致当批失败，也不误入黑名单 |
| 摘要核验 | 结构、年份、逐条事实证据、不合格重写 | 减少截断、年份编造和无依据断言 |
| 配额保护 | 单篇摘要可跳过，但跳过后重新检查媒体/机构保底 | 兼顾局部容错与报告完整性 |
| 备份 | SQLite backup API + 原子替换，默认保留 14 份 | 包含 WAL 已提交事务，避免不一致文件副本 |
| 告警 | 备份、整理、发送失败通过 SMTP 告警 | 增强无人值守运行的可发现性 |
| 仓库保护 | 忽略环境文件、数据库、日志、浏览器缓存和报告产物 | 降低上传 GitHub 时泄露凭据或运行数据的风险 |

Heritage 采用融合方案：远端发现的 RSS 入口用于候选发现，本地 Crawl4AI fallback 用于尝试
恢复静态 403 的正文。CDT 同样只在静态正文失败时调用 Crawl4AI，不把浏览器成本施加到所有来源。


2026-09-22 增补：按用户《主要跟踪智库及媒体》166 项清单审核后新增 53 项来源（去重后与本地 12 项扩源、远端 15 项扩源取并集，配置共 144 项、启用 111 项）。新源单源候选上限统一设为 10，低于原库的 15~35。逐条审核表见 `docs/来源_166项审核表.md`。其中 16 项因 Cloudflare 反爬返回 403 暂未启用，可后续用 `lnc_mode` 浏览器渲染复测。

本轮验证结果：来源配置 144 项全部通过；JSON 无重复键和重复 ID；标准库 `unittest` 共 59 项
全部通过；Python 编译、`pip check` 和 `git diff --check` 通过。测试不调用真实网络、模型或 SMTP，
不代表生产来源可达率、语义准确率或交付 SLA。

详细设计、冲突处理、上线步骤与观察项见：

- `docs/2026-09-14_融合与机制改进记录.md`
- `docs/H2_实施说明.md`
- `docs/源清单统计.md`

以下内容是此前阶段记录，其中 67/54、82/63、48/50 项测试等数字仅代表当时快照。

整理日期：2026 年 9 月 9 日（北京时间）  
项目：境外 AI 安全治理信息智能摘报系统  
范围：本次对话近五小时内完成的优化，约覆盖当日 13:00—18:00；以对话、Git 提交及当前工作区为依据。部分改动已提交，模型配置、分阶段思考和剔除记录等后续改动目前仍在工作区。

## 1. 优化概览

本轮围绕采集效率、来源覆盖、重复 Token 消耗、摘要生成和邮件交付进行了调整，并补充服务器部署说明。

| 方向 | 主要改动 | 实际作用 |
| --- | --- | --- |
| 采集效率 | 默认 6 路来源并行、候选下载前排重、相关度排序 | 减少重复下载，让更相关的候选优先进入采集 |
| 采集稳定性 | 主备代理与兜底线路、故障切换、按错误类型重试 | 降低单个来源或网络线路失败对整轮采集的影响 |
| 采集准确性 | 优先使用文章发布日期、规范 URL、正文指纹去重 | 减少旧文混入和重复入库 |
| 来源扩展 | 2026-09-13 新增 15 项，维护停用来源 | 当前共 82 项配置，63 项启用 |
| 邮件附件 | 原文逐份发送或 ZIP 打包，超过阈值强制打包 | 控制大量原文附件的发送方式 |
| 模型配置 | 显式使用 V4 模型名称，移除未使用字段 | 让配置和实际请求更容易核对 |
| 分阶段思考 | 筛选关闭，摘要及二次压缩开启 | 将思考模式用于最终展示内容的生成 |
| 剔除记录 | 删除不收录原文，保存 URL，默认 7 天到期 | 避免同一无价值文章反复采集和筛选 |
| 部署文档 | 服务器要求、配置、调度、数据源清单和验收说明 | 提供可供部署与交接的项目说明 |

## 2. 采集效率与稳定性

### 2.1 来源并行与请求控制

- 通过 `INGEST_WORKERS=6` 默认并行处理不同来源，可按服务器和代理容量调整。
- 各任务使用独立网络会话，同一域名共享请求间隔控制与 robots 缓存。
- 单个来源失败会记录错误，不中断其他来源任务。
- 增加来源耗时及整轮耗时统计，便于识别慢源。

### 2.2 候选筛选与排重

- 已入库 URL 在候选额度截断前排除，避免重复文章占用抓取名额。
- 根据标题、URL 路径中的 AI 安全治理相关信息以及发布时间排序候选。
- 相关度仅用于排序，不直接替代模型的收录判断。
- 统一规范化 URL，并在入库时结合正文指纹去重。
- 优先使用文章发布日期，减少把修改时间或 Feed 时间当作首次发布时间的情况。

### 2.3 代理及错误处理

- 支持 `PROXY_PRIMARY`、`PROXY_BACKUP` 和旧 `PROXY` 兜底线路。
- 连接、代理或超时故障可触发线路切换。
- 连接异常、超时、429、5xx 等执行退避重试，其他普通 4xx 不反复重试。
- 同一来源连续三个正文候选返回 401/403 时提前停止，减少无效请求。
- robots 可读取时执行禁抓规则；读取失败默认告警并放行，可通过 `INGEST_ROBOTS_FAIL_CLOSED=true` 改为禁止继续。

## 3. 数据源扩展与现状

统计口径：以 `config/sources.json` 中每个来源 ID 为一项配置，不等同于独立机构或网站数量。

| 指标 | 数量 |
| --- | ---: |
| 配置源总数 | 82 |
| 启用 | 63 |
| 停用 | 19 |
| 2026-09-13 本轮新增 | 15 |
| 启用源每轮候选上限之和 | 1480 |

1480 是配置额度之和，不是每轮实际请求数、入库数或日报条数。默认采集最近 2 天，最终日报默认最多保留 20 条。

### 3.1 参考书签新增来源（6 项）

| 来源 | 配置 ID | 方式 |
| --- | --- | --- |
| Ars Technica AI | `ars_ai` | RSS |
| BBC 科技 | `bbc_tech` | RSS |
| CBS News 科技 | `cbs_tech` | RSS |
| 南华早报人工智能专题 | `scmp_ai` | RSS |
| 日本时报人工智能专题 | `japan_times_ai` | 专题页 |
| 独立报 AI 专题 | `independent_ai` | 专题页 |

参考文件为 `Bookmarks (1)`，以上 6 项为参考书签带来的来源，没有将全部书签直接加入启用列表。2026-09-13 本轮另有 15 项扩源（补齐 40 家清单缺项、补官方/国际源、补纽约时报科技），与本表 6 项不重复，完整清单见《服务器部署要求.md》第 10 节。

### 3.2 类型分布

| 配置类型 | 总数 | 启用 | 停用 |
| --- | ---: | ---: | ---: |
| 政府 gov | 16 | 14 | 2 |
| 政府相关 gov_affiliated | 1 | 1 | 0 |
| 智库 thinktank | 29 | 25 | 4 |
| 媒体 media | 18 | 13 | 5 |
| 组织 organization | 18 | 10 | 8 |

采集方式分布：专题/栏目页 59 项、RSS 22 项、Sitemap 1 项；分别启用 46、16、1 项。

对既往检查中持续拒绝访问或反复超时的来源暂停启用，配置保留以便替换入口和复测。完整 82 项清单、地区分布及 19 项停用说明见《服务器部署要求.md》。启用不代表每轮都有新增文章，也不代表所有页面均已验证可达。

## 4. 邮件原文附件策略

新增可配置的原文发送方式：

```dotenv
MAIL_ORIGINALS_ZIP=false
MAIL_ORIGINALS_ZIP_THRESHOLD=10
```

- 开关为 `true`：本次原文统一打包为 ZIP。
- 开关为 `false`：原文逐个作为附件；数量严格超过阈值时仍强制打包。
- 默认 10 份原文逐个发送，11 份打包。阈值为 0 时，只要有原文就打包。
- 两份摘要合集始终独立附上，不计入原文阈值。
- ZIP 仅包含本次原文，保留分类目录；本地 Word 原文仍保留。
- 无原文时不创建空 ZIP；仅生成报告、不发邮件时不打包。

该改动接入已有的“新闻媒体信息”和“机构信息”双合集及逐篇原文输出流程。

## 5. 模型配置纠正与分阶段思考

### 5.1 模型名称和配置用途

针对旧配置名称与服务端响应模型标识不一致的问题，将默认配置改为显式模型名称 `deepseek-v4-flash`，移除未被流程使用的 `DEEPSEEK_REASONER_MODEL`。

日志记录请求模型、思考模式和响应模型，便于核对。模型名称与思考模式分别控制，不依赖旧名称表达思考开关。

### 5.2 按阶段分配思考模式

| 阶段 | 当前配置 | 思考模式 |
| --- | --- | --- |
| 语义筛选、分类和重要性判断 | `DEEPSEEK_FILTER_THINKING_MODE=disabled` | 关闭 |
| 摘要生成 | `DEEPSEEK_SUMMARY_THINKING_MODE=enabled` | 开启 |
| 超长摘要二次压缩 | 同摘要配置 | 开启 |

两个阶段仍共用 `DEEPSEEK_CHAT_MODEL`。通用的 `DEEPSEEK_THINKING_MODE` 仅作为未指定模式的客户端默认值，不覆盖阶段配置。

阶段模式逐次传入，不修改共享客户端状态，避免摘要开启思考后影响后续筛选。开启思考时不发送 temperature 参数；摘要取最终内容，不把内部思考内容写入报告。

此分配用于控制大批量筛选开销，并给摘要整理提供思考过程；质量改善与耗时变化尚未通过真实样本对照量化。

## 6. 不收录文章的限期剔除机制

### 6.1 处理流程

1. 模型明确返回布尔值 `in_scope=false`。
2. 在同一数据库事务中保存规范化 URL、剔除理由和 UTC 时间，并删除候选库中的对应原文。
3. 下次批量筛选命中该 URL 时直接跳过，不请求模型。
4. 爬虫跨来源共用有效剔除 URL 集合，在下载已知链接正文前跳过。
5. 入库入口再次检查，防止重新导入等路径把有效期内 URL 写回候选库。

新增 SQLite 表：`rejected_articles`。仅保留 URL、理由和时间，不保留被剔除文章的整篇正文。

### 6.2 到期清理

```dotenv
REJECTION_RETENTION_DAYS=7
```

- 保留天数必须为正整数，从记录时间起按实际 UTC 时间计龄。
- 每次采集或批量筛选启动时自动清理已满保留天数的记录。
- 重复命中不延长有效期，避免旧记录因重复出现而长期无法到期。
- 程序停运期间不执行后台清理，下次运行补做清理。
- 到期后允许再次采集；已删除原文不会自动恢复。

### 6.3 适用边界

- 模型超时、JSON 异常、缺少或返回非布尔类型的 `in_scope`，均按判定失败保留重试，不拉黑。
- 被判定收录但因日报条数配额未入选的文章，不进入黑名单。
- 新别名链接只有在解析到相同规范 URL 后才能识别，不能保证所有新 URL 都在下载前识别为同一文章。
- 不自动回填历史日志中的剔除结果，从后续明确判定开始积累。
- 不删除历史 Word 或已导出 JSONL；重新导入仍受有效 URL 记录拦截。

## 7. 部署与交付文档

新增《服务器部署要求.md》，覆盖：

- 单机批处理架构、CPU/内存/磁盘和 Python 环境要求。
- 来源网站、模型 API、SMTP 出站网络及账号配置。
- Windows 部署、定时任务与无人值守运行说明，Linux 适配参考。
- 数据目录、备份、日志、容量监测和验收步骤。
- 全部数据源清单、统计口径、停用原因及验证边界。

部署建议为试运行 2 vCPU / 4 GB 内存，持续运行建议 4 vCPU / 8 GB，无需本地 GPU。该建议是部署起点，不是基于生产负载完成的容量保证。

## 8. 本轮主要配置汇总

```dotenv
# 来源并行数
INGEST_WORKERS=6

# 原文邮件附件
MAIL_ORIGINALS_ZIP=false
MAIL_ORIGINALS_ZIP_THRESHOLD=10

# 模型与阶段思考模式
DEEPSEEK_CHAT_MODEL=deepseek-v4-flash
DEEPSEEK_THINKING_MODE=disabled
DEEPSEEK_FILTER_THINKING_MODE=disabled
DEEPSEEK_SUMMARY_THINKING_MODE=enabled

# 不收录 URL 的保留天数
REJECTION_RETENTION_DAYS=7
```

这是本轮主要字段示例，不包含密钥和邮件凭据。修改 `.env` 后需重新启动进程；同名系统环境变量优先于 `.env`，部署时应同步检查。

## 9. 验证记录与结论范围

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 爬虫及邮件附件阶段测试 | 16 项通过 | 并行、候选排重、时间选择、数据库去重及邮件附件等 |
| 模型配置纠正后测试 | 19 项通过 | 增加请求模型、思考参数和非法配置检查 |
| 分阶段思考后测试 | 21 项通过 | 增加阶段切换、摘要二次压缩及状态隔离检查 |
| 剔除机制后测试 | 25 项通过 | 增加删除、重复拦截、异常保留、到期恢复和收录文章保留检查 |
| Git 差异格式检查 | 通过 | `git diff --check` |

此前本地采集健康检查：2026-09-09 15:19，42 个启用源、6 路并行、最近 2 天、每源最多 1 个候选，临时数据库新增 12 篇、记录错误 0、采集阶段耗时 18.4 秒。

该健康检查未覆盖默认全量候选、DeepSeek 筛选摘要及真实邮件发送，不能把 18.4 秒作为完整日报耗时。近期模型分阶段和剔除机制使用模拟 API 与临时数据库验证，未重新执行真实模型全流程。当前也没有可用于宣称准确率提升百分比、Token 节省比例或生产 SLA 的对照数据。

## 10. 改动定位与后续观察

| 文件或目录 | 本轮主要职责 |
| --- | --- |
| `ai_digest/ingest/crawler.py` | 网络处理、候选优先级、日期解析与排重 |
| `ai_digest/ingest/run.py` | 来源并行、统计和剔除 URL 加载 |
| `config/sources.json` | 来源扩展及启停维护 |
| `ai_digest/db.py` | 入库去重、剔除记录和原文删除事务 |
| `ai_digest/filter/` | 严格判定校验、筛选模式和剔除处理 |
| `ai_digest/llm/deepseek.py` | 模型请求、逐次思考开关及日志 |
| `ai_digest/summarize/run.py` | 摘要与压缩阶段开启思考 |
| `ai_digest/report/pipeline.py`、`ai_digest/deliver/emailer.py` | ZIP 附件选择和邮件附件处理 |
| `ai_digest/config.py`、`.env.example` | 新配置项及默认值 |
| `tests/` | 采集、邮件、模型参数及剔除机制测试 |
| `README.md`、`服务器部署要求.md` | 使用和部署说明 |

Git 记录包括当日 14:28 的 `6a7c2ae`（修正）及 16:38 的 `12d79fe`（爬虫优化1.0）；12:53 的 `e227a7b` 是本轮相邻时间段的双代理、双报告及压缩包雏形背景。后续工作区改动尚未形成新提交，不能仅凭提交历史推断全部功能状态。

下一阶段建议观察实际日报的筛选与摘要耗时、剔除命中量、模型 Token 用量，以及人工抽样发现的误剔除和摘要事实偏差，再决定是否调整来源候选额度、提示词和保留天数。这些是待观察事项，不属于本轮已经完成的质量评估。

---

# 近期优化总结（2026-09-09 晚 — 2026-09-10）

整理日期：2026 年 9 月 10 日（北京时间）
项目：境外 AI 安全治理信息智能摘报系统
衔接说明：上一节《项目近五小时优化总结》覆盖 09-09 日间至 18:06 的提交 `1aad38e`（项目机制优化1.0）。
本节记录该提交之后仍在工作区、尚未提交的改动，以及 09-10 新增的维护性调整。
证据范围：当前工作区代码、`git status` / `git diff`、`python -m unittest discover -s tests`（48 项通过）。
模型与耗时数据来自模拟调用和既有日志，不代表生产性能或摘要准确率承诺。

## 1. 本轮概览

| 方向 | 主要改动 | 实际作用 |
| --- | --- | --- |
| 交付拆分 | 整理与发送分离：新增就绪指针 `--ready-file`、独立发送入口 `send_ready.py` 与 `--send-prepared` | 可提前整理邮件，发送阶段不再现场生成 |
| 发送预检 | `send_ready.py --check` 只校验清单与附件、打印主题和附件大小 | 发送前确认本期内容，不触发发信 |
| 可观测性 | 新增 `ai_digest/audit.py` 结构化操作日志与 `scripts/inspect_operations.py` | 按运行 ID 关联阶段耗时、模型用量与失败重试 |
| 耗时显示 | 摘要结束打印“摘要生成耗时、本次整理总耗时”，发送结束打印发送耗时 | 控制台直接可见单次运行耗时，无需翻日志 |
| 摘要质量 | 结构完整性校验 + 语义/事实审核 + 不合格重写一次 | 避免半句截断、否定丢失、状态篡改与残缺摘报 |
| 配额保障 | 分类保底、机构权重、逐日回溯补足 | 媒体/机构两类下限不再被总篇数截断挤掉 |
| 模型调用 | 关闭 SDK 隐式重试、记录 finish_reason 与 Token 用量 | 便于核对真实请求次数、结束原因与用量 |
| 来源扩充 | 来源配置 67→82，启用 54→63 | 2026-09-13 补齐 40 家清单缺项、官方/国际源与纽约时报科技 |
| 测试调度 | 半点采集+准备、整点发送的测试定时任务脚本 | 上线前按小时滚动验证全链路 |
| 仓库准备 | README 指令详解、`.gitignore` 归类收紧、本总结 | 便于交接与上传 GitHub |

## 2. 整理与发送彻底分离

整理阶段（不加 `--send`）完成筛选、摘要、双合集、逐条原文及必要压缩包，并生成不可变的
`mail_manifest.json`：内含主题、正文、附件相对路径和每份附件的 SHA-256。发送阶段只读取
该清单，不调用模型、不查询数据库、不生成 Word 或压缩包。

```powershell
# 只整理并写入就绪指针
python scripts/run_daily.py --input db --ready-file data/manual-ready.txt

# 按时发送（或发送前用 --check 预检）
python scripts/send_ready.py --ready-file data/manual-ready.txt --check
python scripts/send_ready.py --ready-file data/manual-ready.txt

# 等价发送入口：直接给出清单绝对路径，需与 --send/--hours/--at/--ready-file 互斥
python scripts/run_daily.py --send-prepared "C:\path\to\mail_manifest.json"
```

发送前的附件校验会拒绝缺失或被改动的文件（SHA-256 不一致即中止），避免发出不完整邮件。
重复发送同一清单会再次发信，属于显式行为。旧 `--send` 仅保留为“整理成功后立即发送本期”
的一次性联调入口，正式运行使用分离方式。

`run_daily.py --ready-file` 会先删除旧指针再开始整理，只有整理成功才原子写入新指针；
整理失败时保持“无就绪文件”状态，防止误发上一期旧批次。每次整理写入独立子目录
`report_output/<日期>/<时分秒>_<随机串>/`，不覆盖待发送附件。

## 3. 就绪指针与定时任务

正式的每日任务互相独立：

- `AIDigestPrepare`：每天 06:05 执行 `scripts/prepare_daily.py`，写 `data/ready-daily-YYYYMMDD.txt`。
- `AIDigestDaily`：每天 07:00 执行 `scripts/send_ready.py`，只读取当天指针发送。

统计窗口仍为北京时间 `[昨日06:00, 今日06:00)`。安装或更新任务使用
`scripts/install_task.ps1`（管理员）。就绪文件缺失时发送任务直接报错，不回退历史批次，
也不现场生成；整理耗时若逼近发送时间，应提前安排整理或后移发送时间。

## 4. 操作日志与耗时显示

新增 `ai_digest/audit.py`：以 `operation(stage, **details)` 上下文记录开始/完成/失败事件，
写入按 UTC 日期追加的 `logs/operations-YYYYMMDD.jsonl`。同一线程内嵌套阶段共享 `run_id`，
每条事件含 `time`、`stage`、`status`、`elapsed_seconds`；异常记录类型，不记录密钥，
开启 `LLM_LOG_RESPONSE` 时仅记录模型最终正文、不记录思考过程。

本轮新增的耗时显示：

- 整理结束时打印“摘要生成耗时”和“本次整理总耗时”；`summary` 阶段日志同时写入 `summary_seconds`。
- 发送结束时打印“发送完成，耗时”，与 `mail.send` / `delivery.command` 日志中的耗时对应。
- `total_seconds` 覆盖整条流水线（判定、摘要、生成文件、准备清单，若带 `--send` 还包括发送）。

```powershell
# 最近一次运行的模型、Token、失败次数与阶段耗时
python scripts/inspect_operations.py
# 指定运行并展开每次模型响应
python scripts/inspect_operations.py --run-id <run_id> --responses
```

`llm_elapsed_seconds` 是请求耗时之和；阶段相互嵌套，不能把所有阶段耗时相加当作总耗时，
总耗时以 `report` / `daily.command` 事件或控制台输出为准。

## 5. 摘要完整性与事实审核

针对“字数合格但正文结束在半句话”的问题，摘要改为三层校验：

1. 模型响应 `finish_reason` 必须正常结束，否则按失败重试。
2. 正文结构检查：类型、500 字符硬上限、完整句尾、括号引号配对。
3. 基于原文输入的语义与事实审核，识别“残句后补句号”、否定丢失、政策状态改变等问题。

审核未通过时携带原文与问题重写一次，重写固定关闭思考；仍不合格则抛错中止准备，
不缓存、不导出残缺摘报，也不再使用过去的 `body[:500]` 硬截断。合格结果按
提示词、模型、思考配置与输入缓存到 `data/summary_cache/`（`QUALITY_VERSION=2`），
配置或内容变化自动失效。审核使用同一模型，不能保证发现全部事实错误；正文输入仍然
限制在原文前 6000 字符，不等同于全文事实核查。

## 6. 分类保底与历史补足

正式数据库出报要求媒体、机构各至少 5 篇合格材料，总篇数默认仍为 20。先按重要度排序；
同重要度内机构来源评分乘以 1.2，再按时间新→旧。排序先为两类各保留 5 个名额，再分配
剩余名额，避免机构材料在总数截断时被挤出。

初始窗口不足时，从窗口起点逐日向前扩展最多 30 天，仅处理未达保底数量的类别：先读取
该历史日的数据库材料，仍不足时按该日时间窗抓取缺少类别的启用来源，再读库。选择过程
按 URL 去重、不复用同一 URL 判定，质量判定与黑名单不放宽。补足失败会抛出
`InsufficientMaterials` 并停止文件准备与发送；失败的重新准备会清除旧就绪指针。

配置：`REPORT_MIN_PER_GROUP=5`、`REPORT_MAX_EXPANSION_DAYS=30`、`REPORT_INSTITUTION_WEIGHT=1.2`。
总篇数上限必须不少于两类保底数量之和。操作日志新增 `selection` 与 `selection.expand` 阶段。

## 7. 模型调用、Token 用量与失败处理

`DeepSeekClient` 关闭 OpenAI SDK 的隐式重试（`max_retries=0`），重试逻辑由本地按次数控制。
每次调用记录 `llm.request`：请求/响应模型、响应 ID、`finish_reason`、输入/输出/合计 Token、
服务端报告的缓存与推理 Token、耗时以及（可选）完整最终响应。服务端未报告的用量记为
`null`，不冒充 0；失败重试中已报告的用量同样保留。空正文或异常结束原因会触发失败处理。

`LLM_LOG_RESPONSE=true`（默认）记录模型原始最终响应，可设 false 关闭该字段。
筛选与摘要共用 `DEEPSEEK_CHAT_MODEL`，两个阶段的思考模式分别配置，摘要阶段默认
`DEEPSEEK_SUMMARY_THINKING_MODE=disabled`。

## 8. 数据来源扩充

来源配置总数由 53 增至 67，启用由 42 增至 54。新增启用 12 项：OpenAI 新闻与研究、
Anthropic 研究与新闻、Google DeepMind、Stanford HAI、METR、Apollo Research、GovAI、
Hugging Face 博客，以及 TechCrunch AI、MIT Technology Review AI、WIRED AI。
新增但暂不启用 2 项：Lawfare RSS 返回 403；Anthropic 对齐博客缺少稳定日期元数据。
采集继续遵守域名白名单、robots 与原有安全治理筛选标准，不因扩源降低收录门槛。

## 9. 测试阶段定时任务

`scripts/install_test_tasks.ps1` 注册两个按小时运行的任务，包装脚本为
`scripts/run_scheduled_test.ps1`：

- `AISecurityBrief-Test-crawl`：每小时 30 分先执行 `run_crawl.py`，再用最近 48 小时数据
  执行 `run_daily.py --input db --hours 48 --ready-file data/ready-test-YYYYMMDD-HH.txt`。
- `AISecurityBrief-Test-send`：每小时 00 分只读取本小时就绪文件发送，不调用模型。

采集成功后才准备下一整点批次；半点批次未完成则该整点报错、不发送历史文件，也不自动顺延。
控制台日志位于 `logs/test-crawl-*.log` / `logs/test-send-*.log`。测试窗口可能重复包含
已收录文章，目前尚无“已发送文章永久排除”机制；测试结束后应暂停这两个任务。

## 10. 交付文档与仓库准备

- `README.md` 新增《提前整理与按时发送（指令详解）》，覆盖只整理、发送前预检、
  按时发送、整理并立即发送、定时任务、退出码与耗时六部分，并修正目录结构与默认思考模式说明。
- `.gitignore` 按“密钥与凭据 / 运行时数据与产物 / Python / 编辑器与系统 / 本地参考文件”
  重新归类：忽略 `.env` 及 `.env.*`（保留 `.env.example`）、密钥文件、`data/`、`logs/`、
  `report_output/`、`tmp/`、数据库文件（含 WAL/SHM）、缓存与 `Bookmarks (1)` 等。
- 本文件追加本节，记录 09-09 晚以来的改动。

上传 GitHub 前已核查：仓库内除 `.env.example` 外没有提交 `.env`、密钥或真实密钥文件，
未跟踪文件与改动中不含 `sk-` 形式的密钥；`report_output/`、`logs/`、`data/` 不进入版本库。

## 11. 验证记录

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| `python -m unittest discover -s tests` | 48 项通过 | 摘要缓存与失效、审核不通过不缓存、清单校验、就绪指针、配额、扩充、操作日志汇总、采集与发信 |
| `git diff --check` | 通过 | 无空白错误 |
| 工作区密钥扫描 | 未发现泄露 | 除 `.env`（已忽略）外无真实密钥 |

本轮未执行真实模型全流程与真实邮件投递，耗时与 Token 数据不能作为生产容量结论。

## 12. 改动定位

| 文件或目录 | 本轮主要职责 |
| --- | --- |
| `ai_digest/audit.py` | 结构化操作日志、`run_id` 关联、`format_duration` 时长格式化 |
| `ai_digest/deliver/prepared.py` | 清单生成、附件校验（`load_prepared`）与发送 |
| `ai_digest/report/selection.py`、`history.py` | 分类保底、逐日回溯与历史补抓 |
| `ai_digest/report/pipeline.py`、`rank.py` | 流水线编排、摘要耗时与总耗时统计、配额排序 |
| `ai_digest/summarize/` | 摘要生成、完整性/事实审核与缓存 |
| `ai_digest/llm/deepseek.py` | 请求重试控制与 Token/耗时日志 |
| `scripts/run_daily.py`、`prepare_daily.py`、`send_ready.py` | 整理/发送分离、就绪指针、发送预检与耗时输出 |
| `scripts/inspect_operations.py`、`install_test_tasks.ps1`、`run_scheduled_test.ps1` | 日志汇总与测试期定时任务 |
| `tests/test_preparation.py`、`test_operation_report.py`、`test_quota.py`、`test_summary_integrity.py` | 新增回归测试 |
| `README.md`、`.gitignore`、`项目优化总结.md` | 指令文档、仓库忽略规则与总结 |

## 13. 待观察事项与边界

- 真实日报的筛选与摘要耗时、模型 Token 用量、返工率需以生产日志为准；当前 48 项测试
  未覆盖真实模型响应质量与真实 SMTP 投递失败重试。
- 分类保底依赖历史网页可发现范围，最多回溯 30 天不保证覆盖完整历史；来源公开列表不足时
  会中止准备，这是刻意保留的失败边界。
- 摘要审核仍可能漏检事实错误；`--check` 只校验附件完整性与清单内容，不校验摘报文字质量。
- `report_output/` 默认不提交；如需归档某期摘报，使用 `git add -f <路径>` 显式加入。
